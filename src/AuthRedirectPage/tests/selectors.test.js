// import { fromJS } from 'immutable';
// import { makeSelectAuthRedirectPageDomain } from '../selectors';

// const selector = makeSelectAuthRedirectPageDomain();

describe('makeSelectAuthRedirectPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
